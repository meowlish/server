services:
  pgadmin:
    image: dpage/pgadmin4:9.8.0
    restart: always
    ports:
      - ${PGADM_PORT:-80}:80
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADM_MAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADM_PASS}
    networks:
      - api-network
