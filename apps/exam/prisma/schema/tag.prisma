model TagTableLock {
  version Int @id @default(0)

  @@map("tags_table_lock")
}

model Tag {
  id   String @id @default(uuid())
  name String @unique
  lft  Int
  rgt  Int

  @@index([lft, rgt])
  @@index([name])
  @@map("tags")
}
